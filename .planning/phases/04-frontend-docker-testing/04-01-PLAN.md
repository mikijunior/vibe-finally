---
phase: 04-frontend-docker-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/tsconfig.json
  - frontend/postcss.config.mjs
  - frontend/.eslintrc.json
  - frontend/.prettierrc
  - frontend/.prettierignore
  - frontend/src/app/layout.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/globals.css
  - frontend/src/lib/api.ts
  - frontend/src/lib/types.ts
  - frontend/src/lib/format.ts
  - frontend/src/lib/store.ts
  - frontend/src/lib/price-stream.ts
  - frontend/src/lib/hooks/useWatchlist.ts
  - frontend/src/lib/hooks/usePortfolio.ts
  - frontend/src/lib/hooks/useSelectedTicker.ts
  - frontend/src/components/WatchlistPanel.tsx
  - frontend/src/components/Sparkline.tsx
  - frontend/src/components/MainChart.tsx
  - frontend/src/components/PriceCell.tsx
  - frontend/src/components/PriceStreamProvider.tsx
  - frontend/src/styles/flash.css
  - frontend/.gitignore
  - .gitignore
autonomous: true
requirements:
  - FE-01
  - FE-02
  - FE-03
  - FE-04
  - FE-05
  - WL-01
  - WL-02
  - WL-03
  - WL-04
  - WL-05
  - WL-06
  - CH-01
  - CH-02
  - CH-03
  - CH-04

must_haves:
  truths:
    - Visiting localhost:8000 renders a dark terminal dashboard with header, watchlist, main chart area, and sidebar/footer placeholders for portfolio + chat
    - The watchlist displays the 10 default seeded tickers (AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX) with live prices that flash green on uptick and red on downtick when SSE updates arrive
    - Each watchlist row contains a canvas sparkline that progressively fills with points accumulated from the SSE stream since page load
    - Clicking a watchlist row selects that ticker and updates the main Lightweight Charts chart to show its streaming price line
    - The user can add a ticker via an input + button (POST /api/watchlist) and remove a ticker via a row delete button (DELETE /api/watchlist/{ticker}); the watchlist refreshes without a full page reload
    - EventSource auto-reconnects on disconnect (built-in browser retry directive honored)
  artifacts:
    - frontend/ (full Next.js 15 project with TypeScript, Tailwind v4, App Router, src/ no, output: 'export')
    - frontend/package.json with locked deps: next@15, react@19, typescript@5, tailwindcss@4.3, @tailwindcss/postcss@4.3, zustand@5, lightweight-charts@5.2.0, swr@2, clsx, lucide-react, prettier-plugin-tailwindcss (dev)
    - frontend/next.config.ts with output: 'export', images.unoptimized: true, trailingSlash: false
    - frontend/src/app/layout.tsx with html className dark, color-scheme CSS
    - frontend/src/app/page.tsx as the workstation dashboard composition
    - frontend/src/app/globals.css with Tailwind v4 @import and @theme directive mapping --color-accent-yellow #ecad0a, --color-blue-primary #209dd7, --color-purple-secondary #753991, --color-bg-base #0d1117, --color-bg-elevated #1a1a2e
    - frontend/src/lib/api.ts with typed fetch wrappers for /api/portfolio, /api/portfolio/trade, /api/portfolio/history, /api/watchlist (GET/POST/DELETE), /api/chat, /api/health
    - frontend/src/lib/types.ts with explicit interfaces matching backend Pydantic schemas
    - frontend/src/lib/store.ts with a Zustand price store: prices, sparklines, lastDirection, version
    - frontend/src/lib/price-stream.ts with usePriceStream() hook: connects EventSource('/api/stream/prices'), parses event.data JSON, exposes connection status
    - frontend/src/components/WatchlistPanel.tsx: grid/table of ticker rows with PriceCell + Sparkline, click selects ticker, inline delete button, add-ticker input+button at top
    - frontend/src/components/Sparkline.tsx: canvas-based mini-chart via Lightweight Charts areaSeries, uses series.update() per new point
    - frontend/src/components/MainChart.tsx: large Lightweight Charts areaSeries chart for the currently selected ticker, uses series.update() per new SSE point
    - frontend/src/components/PriceCell.tsx: displays price + change %, applies .flash-up or .flash-down CSS class for ~500ms when direction changes
    - frontend/src/components/PriceStreamProvider.tsx: client component that mounts usePriceStream() once at root
    - frontend/src/styles/flash.css with .flash-up / .flash-down keyframes that fade green/red over ~500ms
    - frontend/src/lib/hooks/useWatchlist.ts: SWR hook for /api/watchlist with mutate
    - frontend/src/lib/hooks/useSelectedTicker.ts: Zustand store for the currently selected ticker
    - frontend/out/ static export produced by `npm run build`
  key_links:
    - EventSource('/api/stream/prices') -> parses data: {ticker: PriceUpdate, ...} -> store.bulkUpdate(ticker, update) -> triggers React re-render of WatchlistPanel and MainChart
    - price-stream hook -> on SSE event: pushes price into sparklines[ticker] (cap at 60 points) AND calls store.update so PriceCell flashes and Sparkline appends
    - WatchlistPanel row click -> useSelectedTicker.set(ticker) -> MainChart re-renders with new ticker subscription
    - useWatchlist mutate -> POST /api/watchlist triggers backend to also call market_source.add_ticker, which emits a new ticker into SSE -> frontend receives it via price-stream hook
---

<objective>
Scaffold the FinAlly frontend with Next.js 15 + Tailwind v4 + Zustand, then implement the watchlist panel, sparkline mini-charts, the main Lightweight Charts chart, and the EventSource SSE price-streaming hook. Plan 04-02 builds on this to add portfolio visualizations, trade bar, chat, and header; Plan 04-03 wraps everything in Docker and adds E2E tests.

Purpose: This plan ships the first vertical slice the user sees — a live-updating terminal dashboard with a clickable ticker chart. No trade bar, chat, or portfolio viz yet (those come in 04-02), but the data plumbing (api.ts, price-stream.ts, store.ts) is established so 04-02 components just consume it.

Output: A `frontend/` directory containing a buildable Next.js static export that renders a dark dashboard, streams live prices, supports watchlist add/remove, and shows a click-to-select main chart.
</objective>

<execution_context>
@/Users/mijunior/vibecode/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/mijunior/vibecode/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/mijunior/vibecode/finally/.planning/PROJECT.md
@/Users/mijunior/vibecode/finally/.planning/ROADMAP.md
@/Users/mijunior/vibecode/finally/.planning/STATE.md
@/Users/mijunior/vibecode/finally/.planning/REQUIREMENTS.md
@/Users/mijunior/vibecode/finally/planning/PLAN.md
@/Users/mijunior/vibecode/finally/backend/app/api/schemas.py
@/Users/mijunior/vibecode/finally/backend/app/market/stream.py
@/Users/mijunior/vibecode/finally/backend/app/db/seed.py
@/Users/mijunior/vibecode/finally/backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 frontend with locked stack and theme tokens</name>
  <files>
    - frontend/package.json
    - frontend/next.config.ts
    - frontend/tsconfig.json
    - frontend/postcss.config.mjs
    - frontend/.eslintrc.json
    - frontend/.prettierrc
    - frontend/.prettierignore
    - frontend/.gitignore
    - .gitignore
  </files>
  <read_first>
    - frontend does not exist; run `ls /Users/mijunior/vibecode/finally/frontend 2>&1` first to confirm absence and abort if it does (someone else may have scaffolded). Do NOT run create-next-app non-interactively in a way that puts a git repo inside frontend; pass `--no-git` and `--use-npm`.
    - frontend/src/lib/api.ts is referenced by every downstream component; lock its path before scaffolding so component imports stay stable.
  </read_first>
  <action>
    1. From `/Users/mijunior/vibecode/finally`, scaffold the project non-interactively:
       `npx --yes create-next-app@latest frontend --typescript --tailwind --eslint --app-router --no-src-dir --import-alias "@/*" --use-npm --no-git`
       This produces Next.js 15.x with Tailwind v4 by default, App Router, TypeScript, ESLint. Use `--no-src-dir` so the layout files land directly under `frontend/app/` per CLAUDE.md recommendation (then we move them to `frontend/src/app/` in the next step).
    2. After scaffold completes, move the contents of `frontend/app/` to `frontend/src/app/` (create `frontend/src/` first). Update any tsconfig paths if necessary so `@/*` resolves to `./src/*`.
    3. In `frontend/package.json`, install the locked-stack dependencies (use `npm install --save` and `npm install --save-dev`):
       - Production deps: `tailwindcss@4.3`, `@tailwindcss/postcss@4.3`, `zustand@5`, `lightweight-charts@5.2.0`, `swr@2`, `clsx`, `lucide-react`. The `next`, `react`, `react-dom`, `typescript` packages installed by create-next-app are kept as scaffolded (verify next is 15.x and react is 19.x; if not, run `npm install next@15 react@19 react-dom@19`).
       - Dev deps: `prettier`, `prettier-plugin-tailwindcss`.
    4. Create `frontend/next.config.ts` with:
       ```
       import type { NextConfig } from "next";
       const nextConfig: NextConfig = {
         output: "export",
         trailingSlash: false,
         images: { unoptimized: true },
       };
       export default nextConfig;
       ```
       Note: `trailingSlash: false` is required for FastAPI static catch-all routing to resolve subpaths correctly.
    5. Update `frontend/tsconfig.json` so `paths` reads `"@/*": ["./src/*"]` (matching the import alias).
    6. Create `frontend/.prettierrc` with `{"semi": true, "singleQuote": false, "plugins": ["prettier-plugin-tailwindcss"]}` and `frontend/.prettierignore` listing `node_modules/`, `out/`, `.next/`.
    7. Append frontend build artifacts to the top-level `.gitignore`:
       ```
       frontend/node_modules/
       frontend/.next/
       frontend/out/
       frontend/.turbo/
       ```
    8. Run `cd frontend && npm run build` to confirm the scaffold compiles. The output directory should be `frontend/out/`. Fix any type errors before proceeding.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npm run build 2>&1 | tail -20 && ls frontend/out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/` directory exists with `package.json`, `next.config.ts`, `tsconfig.json`, `src/app/layout.tsx`, `src/app/page.tsx`
    - `cat frontend/package.json | grep -E '"(next|react|tailwindcss|zustand|lightweight-charts|swr|clsx|lucide-react)"'` lists all locked deps
    - `cd frontend && npm run build` exits 0 and produces `frontend/out/index.html`
    - `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors)
  </acceptance_criteria>
  <done>Frontend project scaffolded and the locked dependency stack installed; TypeScript types compile clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build api.ts, types.ts, Zustand store, and EventSource price-stream hook</name>
  <files>
    - frontend/src/lib/types.ts
    - frontend/src/lib/api.ts
    - frontend/src/lib/format.ts
    - frontend/src/lib/store.ts
    - frontend/src/lib/price-stream.ts
    - frontend/src/lib/hooks/useWatchlist.ts
    - frontend/src/lib/hooks/useSelectedTicker.ts
    - frontend/src/lib/hooks/usePortfolio.ts
    - frontend/src/components/PriceStreamProvider.tsx
    - frontend/src/styles/flash.css
  </files>
  <read_first>
    - Read `backend/app/api/schemas.py` to confirm exact response shapes; if any field name drifts, update types.ts to match (the backend is the source of truth).
    - Read `backend/app/market/stream.py` to confirm SSE event payload shape: `data: {ticker: {ticker, price, previous_price, timestamp, change, change_percent, direction}, ...}` (one event per cache version bump, contains ALL tickers' current state, not a single ticker).
  </read_first>
  <action>
    1. Create `frontend/src/lib/types.ts` with explicit TypeScript interfaces matching backend Pydantic schemas. Include: PriceUpdate, WatchlistEntry, WatchlistResponse, WatchlistMutationResponse, Position, PortfolioResponse, TradeRequest, TradeResponse (with trade: TradeRecord and position: PositionRecord or null), Snapshot, PortfolioHistoryResponse, ChatActionResult, ChatEndpointResponse, ChatRequest, HealthResponse, ConnectionStatus.
    2. Create `frontend/src/lib/api.ts` with native `fetch`-based wrappers. Export: getPortfolio, executeTrade, getPortfolioHistory, getWatchlist, addWatchlistTicker, removeWatchlistTicker, sendChat, getHealth. Each throws an Error with the server's `detail` message on non-2xx (parse JSON body and use `.detail` field). Use `Content-Type: application/json` for POSTs; DELETE has no body. Normalize ticker to uppercase before sending to match backend validator. Use absolute URL based on `window.location.origin`.
    3. Create `frontend/src/lib/format.ts`: formatDollars(n) returns "$1,234.56", formatPercent(n, digits=2) returns "+1.23%" for positives and "-1.23%" for negatives (zero renders as "0.00%" without sign), formatChange(n) returns signed dollar change like "+$1.23", formatQuantity(n) returns "1.50".
    4. Create `frontend/src/lib/store.ts` — a Zustand store with: prices (Record<string, PriceUpdate>), sparklines (Record<string, number[]> capped at 60 points, newest-last), lastDirection (Record<string, "up"|"down"|"flat">), version (number). Methods: update(ticker, PriceUpdate) appends price to sparklines and trims to 60; bulkUpdate(updates) iterates and calls update for each; setDirection(ticker, direction) updates lastDirection.
    5. Create `frontend/src/lib/price-stream.ts` exporting `usePriceStream()`. Implementation:
       - On first mount (in PriceStreamProvider client component), opens `new EventSource("/api/stream/prices")`.
       - Tracks `EventSource.readyState` and exposes `connectionStatus: ConnectionStatus` (OPEN -> "connected", CONNECTING -> "reconnecting", CLOSED -> "disconnected").
       - On `message` event: parses `event.data` as `Record<string, PriceUpdate>`, calls `store.bulkUpdate(updates)`.
       - Module-level singleton so reconnects across hot reloads don't leak; cleanup on `beforeunload`.
       - Returns `{ connectionStatus }`; price data accessed via `useStore` directly.
    6. Create `frontend/src/lib/hooks/useWatchlist.ts`:
       - Uses `useSWR<WatchlistResponse>("watchlist", getWatchlist, { refreshInterval: 0, revalidateOnFocus: true })`.
       - Returns `{ entries: data?.entries ?? [], error, isLoading, mutate }`.
    7. Create `frontend/src/lib/hooks/useSelectedTicker.ts` — a Zustand store with `selectedTicker: string | null`, `set(t)`, `clear()`. Default null.
    8. Create `frontend/src/styles/flash.css` with `@keyframes flash-up` (rgba(34,197,94,0.4) -> transparent) and `@keyframes flash-down` (rgba(239,68,68,0.4) -> transparent), each 500ms ease-out. Define `.flash-up` and `.flash-down` classes that apply the animation.
    9. Create `frontend/src/lib/hooks/usePortfolio.ts` (stub for 04-02 compat):
       - `usePortfolio()` returns `useSWR<PortfolioResponse>("portfolio", getPortfolio, { refreshInterval: 5000 })`.
       - `usePortfolioHistory()` returns `useSWR<PortfolioHistoryResponse>("portfolio-history", getPortfolioHistory, { refreshInterval: 30000 })`.
    10. Create `frontend/src/components/PriceStreamProvider.tsx` (client component, `"use client"`):
       - Calls `usePriceStream()` once and renders `{children}`.
       - Provides the SSE connection at the root of the app.
    11. Run `cd frontend && npx tsc --noEmit` to confirm types align.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/lib/types.ts` exists with all named interfaces listed above (PriceUpdate, WatchlistEntry, WatchlistResponse, WatchlistMutationResponse, Position, PortfolioResponse, TradeRequest, TradeResponse, Snapshot, PortfolioHistoryResponse, ChatActionResult, ChatEndpointResponse, ChatRequest, HealthResponse, ConnectionStatus)
    - `frontend/src/lib/api.ts` exists and exports at least 7 fetch-based functions
    - `frontend/src/lib/store.ts` exports a Zustand store with prices, sparklines, lastDirection, version fields and update/bulkUpdate methods
    - `frontend/src/lib/price-stream.ts` exports a `usePriceStream()` hook
    - `frontend/src/components/PriceStreamProvider.tsx` exists as a client component wrapping usePriceStream
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>API client, Zustand price store, types, formatters, SWR hooks, and the EventSource price-stream hook are all in place; the SSE connection is established once at the app root via PriceStreamProvider.</done>
</task>


<task type="auto">
  <name>Task 3: Implement WatchlistPanel, Sparkline, MainChart, PriceCell, and dashboard layout</name>
  <files>
    - frontend/src/components/WatchlistPanel.tsx
    - frontend/src/components/Sparkline.tsx
    - frontend/src/components/MainChart.tsx
    - frontend/src/components/PriceCell.tsx
    - frontend/src/app/layout.tsx
    - frontend/src/app/page.tsx
    - frontend/src/app/globals.css
  </files>
  <read_first>
    - Tailwind v4 syntax: in `globals.css` use `@import "tailwindcss";` then `@theme { --color-accent-yellow: #ecad0a; ... }` (NOT `tailwind.config.ts`). If Tailwind v4 still requires a config in the installed version, fall back to `tailwind.config.ts` with `theme.extend.colors` and document the deviation.
    - Lightweight Charts v5 API: `createChart(container, options)` returns an IChartApi; `chart.addAreaSeries({...})` returns ISeriesApi; use `series.update({ time, value })` per point — time must be a UTCTimestamp (seconds). If a point arrives with the same timestamp as the previous one, skip the update (lightweight-charts requires monotonically non-decreasing times).
  </read_first>
  <action>
    1. Rewrite `frontend/src/app/globals.css` (Tailwind v4):
       - `@import "tailwindcss";`
       - `@theme { --color-accent-yellow: #ecad0a; --color-blue-primary: #209dd7; --color-purple-secondary: #753991; --color-bg-base: #0d1117; --color-bg-elevated: #1a1a2e; --color-border-muted: #2a2f3a; --color-text-primary: #e6edf3; --color-text-muted: #8b95a5; --color-pnl-up: #22c55e; --color-pnl-down: #ef4444; }`
       - `html { color-scheme: dark; background-color: var(--color-bg-base); color: var(--color-text-primary); }`
       - `body { background-color: var(--color-bg-base); }`
       - `@import "../styles/flash.css";`
    2. Update `frontend/src/app/layout.tsx`: set `<html lang="en" className="dark">`, `<body className="bg-bg-base text-text-primary">`, import `./globals.css`, render `<PriceStreamProvider>{children}</PriceStreamProvider>`.
    3. Create `frontend/src/components/PriceCell.tsx`:
       - Props: `ticker: string`
       - Subscribes to `useStore(s => s.prices[ticker])` and `useStore(s => s.lastDirection[ticker])`.
       - Tracks a ref of previous direction; on direction change, set local state `flashClass: "flash-up" | "flash-down" | ""` for 500ms via `setTimeout`, then clear.
       - Renders ticker symbol, `formatDollars(price)`, and `formatPercent(change_percent)` colored green/red based on direction.
    4. Create `frontend/src/components/Sparkline.tsx`:
       - Props: `ticker: string`
       - Subscribes to `useStore(s => s.sparklines[ticker])`.
       - On mount, creates a `div` ref and instantiates a Lightweight Charts chart with width=80, height=32, transparent background, no grid, hidden time/price scales, no scroll/scale handlers.
       - Adds `addAreaSeries({ lineColor, topColor, bottomColor: "transparent" })` where lineColor is green if last change >= 0, else red.
       - On every sparklines array change, iterates points and calls `series.update({ time: Math.floor(ts), value })` for each new point. Track the last time sent to skip duplicates.
       - Cleanup on unmount: `chart.remove()`.
    5. Create `frontend/src/components/WatchlistPanel.tsx`:
       - Reads `useWatchlist()` for entries; renders an input field + "Add" button (calls `addWatchlistTicker` then `mutate()`); for each entry renders a row with `[ticker] [PriceCell] [Sparkline] [delete X button]`.
       - Row onClick: `useSelectedTicker.getState().set(entry.ticker)`. Selected row gets a border in `--color-blue-primary`.
       - Delete button: `onClick={(e) => { e.stopPropagation(); removeWatchlistTicker(entry.ticker).then(() => mutate()); }}`.
       - If watchlist is empty, render a muted placeholder.
       - Top-level element: `<div className="flex flex-col gap-1 p-2 bg-bg-elevated rounded border border-border-muted">`.
    6. Create `frontend/src/components/MainChart.tsx`:
       - Reads `selectedTicker` from `useSelectedTicker`.
       - Subscribes to `useStore(s => s.prices[selectedTicker])` and `useStore(s => s.sparklines[selectedTicker])`.
       - Renders `<div ref={containerRef} className="w-full h-[480px]" />`.
       - On mount + on ticker change: create a new Lightweight Charts chart with proper dark theme, add an area series, then iteratively `series.update()` from the existing sparkline buffer + apply live updates from the price subscription. Use `chart.timeScale().fitContent()` on initial ticker load.
       - Show "Select a ticker" placeholder when `selectedTicker` is null.
    7. Update `frontend/src/app/page.tsx`:
       - Mark as `"use client"` (composes interactive components).
       - Layout: a CSS grid with three regions: header bar (placeholder for 04-02), main area (left: WatchlistPanel; right: MainChart), bottom area (placeholder for portfolio/chat in 04-02).
       - Use Tailwind classes for the grid; assign `--color-bg-base` to the page background.
    8. Run `cd frontend && npm run build` — must produce `frontend/out/` with `index.html`, `_next/` static assets, and exit 0. Fix any TypeScript errors. If build warns about `EventSource` being undefined, wrap the EventSource instantiation in `typeof window !== "undefined"` guards.
    9. After the build succeeds, confirm `ls frontend/out/` shows `index.html` and a `_next/` directory. The static mount to FastAPI is wired in 04-03.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npm run build 2>&1 | tail -25 && echo '---' && ls frontend/out/index.html && echo '---' && cd frontend && npx tsc --noEmit 2>&1 | tail -10 && echo '---' && cd frontend && npx eslint src/ --max-warnings 0 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/WatchlistPanel.tsx`, `Sparkline.tsx`, `MainChart.tsx`, `PriceCell.tsx` all exist
    - `frontend/src/app/layout.tsx` wraps children in `<PriceStreamProvider>`
    - `frontend/src/app/page.tsx` composes `<WatchlistPanel/>` and `<MainChart/>` in a CSS grid with dark theme classes
    - `frontend/src/app/globals.css` contains `@theme` block with all 8 color tokens and `@import "../styles/flash.css"`
    - `cd frontend && npm run build` exits 0 and produces `frontend/out/index.html`
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint src/ --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>WatchlistPanel with add/remove + click-to-select, Sparkline + MainChart using Lightweight Charts series.update(), PriceCell with flash animations, and the dashboard page composition are all rendering against the live SSE stream. The static export at frontend/out/ is produced.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd frontend && npm run build` exits 0 and produces `frontend/out/index.html`
2. `cd frontend && npx tsc --noEmit` exits 0 (no TypeScript errors)
3. `cd frontend && npx eslint src/ --max-warnings 0` exits 0 (no lint errors)
4. With backend running on port 8000, `curl -s http://localhost:8000/api/watchlist` returns a JSON object with `entries` containing all 10 default tickers
5. The page renders without console errors when opened in a browser; watchlist rows show prices that update every ~500ms; sparklines progressively fill; clicking a row changes the main chart; add/remove ticker buttons work end-to-end
</verification>

<success_criteria>
- All 15 requirements (FE-01..05, WL-01..06, CH-01..04) are implemented and verifiable against `curl` + a browser session against the running backend
- The static export at `frontend/out/` is self-contained and runnable behind FastAPI's static mount (wired in 04-03)
- No regressions in the backend: `cd backend && uv run --extra dev pytest -q` still passes
</success_criteria>

<output>
Create `.planning/phases/04-frontend-docker-testing/04-01-SUMMARY.md` when done
</output>
