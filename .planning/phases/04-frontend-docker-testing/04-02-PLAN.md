---
phase: 04-frontend-docker-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/components/PortfolioHeatmap.tsx
  - frontend/src/components/PnLChart.tsx
  - frontend/src/components/PositionsTable.tsx
  - frontend/src/components/TradeBar.tsx
  - frontend/src/components/ChatPanel.tsx
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/lib/hooks/useChat.ts
  - frontend/src/app/page.tsx
  - frontend/src/app/globals.css
autonomous: true
requirements:
  - PF-01
  - PF-02
  - PF-03
  - PF-04
  - PF-05
  - TB-01
  - TB-02
  - TB-03
  - TB-04
  - TB-05
  - TB-06
  - TB-07
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
  - CHAT-06
  - CHAT-07
  - HDR-01
  - HDR-02
  - HDR-03

must_haves:
  truths:
    - Header shows portfolio total value (live-updated from the price cache: cash + sum(quantity * current_price)), cash balance, and a colored connection-status dot reflecting EventSource readyState (green=OPEN, yellow=CONNECTING, red=CLOSED)
    - Portfolio heatmap renders a treemap of positions sized by portfolio weight (qty*current_price / total_value) and colored by P&L (green for profit, red for loss); empty portfolio renders a muted placeholder
    - P&L chart renders a line/area chart of portfolio value over time from /api/portfolio/history with at least one data point after the snapshot loop has run once
    - Positions table shows: ticker, quantity, avg_cost, current_price, unrealized_pnl, pnl_percent per row with profit/loss coloring
    - Trade bar lets the user type a ticker + quantity and click Buy or Sell; the trade is POSTed to /api/portfolio/trade with no confirmation dialog and the response updates the portfolio/watchlist SWR caches; on HTTP 4xx (insufficient cash/shares) the error message is shown inline next to the inputs
    - Chat panel accepts text input and a Send button; shows loading state during POST /api/chat; displays the assistant's message plus inline confirmation chips for each executed trade (e.g., "Bought 10 AAPL @ $191.50") and each watchlist change (e.g., "+ NVDA added"); failed actions render as red chips with the server's detail message
  artifacts:
    - frontend/src/components/Header.tsx with live total value, cash balance, status dot
    - frontend/src/components/PortfolioHeatmap.tsx using SVG treemap (squarify algorithm or simple slice layout)
    - frontend/src/components/PnLChart.tsx using Lightweight Charts areaSeries
    - frontend/src/components/PositionsTable.tsx using shadcn/ui Table primitive
    - frontend/src/components/TradeBar.tsx with ticker input, qty input, Buy/Sell buttons, inline error display
    - frontend/src/components/ChatPanel.tsx with message list, input, send button, loading state, inline action confirmations
    - frontend/src/components/ui/button.tsx, input.tsx, table.tsx (shadcn-style primitives)
    - frontend/src/lib/hooks/useChat.ts with sendMessage, mutate on success
    - frontend/src/app/page.tsx updated to compose Header + PortfolioHeatmap + PnLChart + PositionsTable + TradeBar + ChatPanel in the layout
  key_links:
    - Header total value: usePortfolio() cash + sum(position.quantity * useStore(s => s.prices[ticker]?.price)) -- live recompute on every price tick
    - PortfolioHeatmap reads usePortfolio() positions + useStore prices for current_price (overrides stale server value with live price)
    - PnLChart reads usePortfolioHistory() snapshots array
    - PositionsTable reads usePortfolio() positions
    - TradeBar calls executeTrade() on submit; on success calls usePortfolio().mutate() and useWatchlist().mutate(); on 4xx displays error.detail
    - ChatPanel calls sendChat() on submit; on success appends user+assistant messages to local state, calls usePortfolio().mutate() and useWatchlist().mutate() so executed actions reflect immediately
---

<objective>
Add the portfolio visualization cluster (heatmap + P&L chart + positions table), the trade bar with inline validation errors, the AI chat panel with inline action confirmations, and the header bar with live total value + connection status dot. Build on Plan 04-01's data layer (api.ts, store.ts, hooks) — no backend changes required.

Purpose: Completes the user-facing functionality so the user can monitor their portfolio, execute trades, and chat with the AI assistant — all visible against the live SSE price stream.

Output: Six new components integrated into the dashboard; existing WatchlistPanel + MainChart (from 04-01) remain functional alongside.
</objective>

<execution_context>
@/Users/mijunior/vibecode/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/mijunior/vibecode/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/mijunior/vibecode/finally/.planning/PROJECT.md
@/Users/mijunior/vibecode/finally/.planning/ROADMAP.md
@/Users/mijunior/vibecode/finally/.planning/STATE.md
@/Users/mijunior/vibecode/finally/.planning/REQUIREMENTS.md
@/Users/mijunior/vibecode/finally/planning/PLAN.md
@/Users/mijunior/vibecode/finally/backend/app/api/schemas.py
@/Users/mijunior/vibecode/finally/.planning/phases/04-frontend-docker-testing/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header, PortfolioHeatmap, PnLChart, and PositionsTable</name>
  <files>
    - frontend/src/components/Header.tsx
    - frontend/src/components/PortfolioHeatmap.tsx
    - frontend/src/components/PnLChart.tsx
    - frontend/src/components/PositionsTable.tsx
    - frontend/src/components/ui/button.tsx
    - frontend/src/components/ui/input.tsx
    - frontend/src/components/ui/table.tsx
    - frontend/src/app/globals.css
  </files>
  <read_first>
    - Read 04-01-PLAN.md to confirm existing types in frontend/src/lib/types.ts (Position, PortfolioResponse, Snapshot, PortfolioHistoryResponse) so the new components import the right names.
    - Read backend/app/api/portfolio.py to confirm cash_balance / total_value / positions shape and which fields are P&L (unrealized_pnl, pnl_percent).
  </read_first>
  <action>
    1. Create three shadcn/ui-style primitives (copy-paste, not installed via package) under `frontend/src/components/ui/`:
       - `button.tsx`: a styled `<button>` with variants: default (bg-blue-primary), secondary (bg-bg-elevated border border-border-muted), destructive (bg-pnl-down), ghost (no bg, hover bg-bg-elevated), and a Submit variant (bg-purple-secondary per color spec). Use `clsx` to combine classes. Forward refs.
       - `input.tsx`: `<input>` with bg-bg-base, border-border-muted, focus:border-blue-primary, text-text-primary. Forward refs.
       - `table.tsx`: `<table>`, `<thead>`, `<tbody>`, `<tr>`, `<th>`, `<td>` primitives with bg-bg-elevated headers and border-border-muted row dividers.
       These are local copies (no shadcn CLI needed); do NOT add `@radix-ui/react-*` or any other deps.
    2. Create `frontend/src/components/Header.tsx`:
       - Reads `usePortfolio()` for cash_balance and positions; reads `useStore(s => s.prices)` for live prices.
       - Computes `totalValue = cash_balance + sum(positions.map(p => p.quantity * (prices[p.ticker]?.price ?? p.current_price)))`.
       - Computes `connectionStatus` from the price-stream hook (import `usePriceStream` from `lib/price-stream`).
       - Renders: left = "FinAlly" wordmark in accent-yellow; center = total value in text-primary; right = cash balance + status dot (8px circle, bg-pnl-up if connected, bg-accent-yellow if reconnecting, bg-pnl-down if disconnected) + a small text label.
       - Use `<header>` element with `className="flex items-center justify-between px-4 py-2 bg-bg-elevated border-b border-border-muted"`.
    3. Create `frontend/src/components/PortfolioHeatmap.tsx`:
       - Reads `usePortfolio()` for positions; reads `useStore(s => s.prices)` for live overrides.
       - For each position: weight = quantity * (live price ?? current_price); color = pnl-up if unrealized_pnl >= 0 else pnl-down; opacity = clamp(abs(pnl_percent) / 10, 0.2, 1.0).
       - Layout: implement a simple slice-and-dice treemap (recursive split of rectangle by weight, alternating horizontal/vertical cuts). For an empty portfolio render a placeholder ("No positions yet").
       - Render an SVG with each tile = `<rect x y width height fill={color} fillOpacity={opacity}>` and a `<text>` label showing ticker + pnl_percent.
       - Wrap in a fixed-height container with `className="bg-bg-elevated rounded border border-border-muted p-2"`.
    4. Create `frontend/src/components/PnLChart.tsx`:
       - Reads `usePortfolioHistory()` for snapshots array.
       - Renders `<div ref={containerRef} className="w-full h-64" />`.
       - On mount + data change: create Lightweight Charts chart with dark theme (no `addAreaSeries` color override -- use defaults tuned for dark), add an area series, iteratively `series.update({ time: Math.floor(new Date(recorded_at).getTime() / 1000), value: total_value })` for each snapshot.
       - Render a placeholder "Waiting for first snapshot..." if snapshots array is empty.
    5. Create `frontend/src/components/PositionsTable.tsx`:
       - Reads `usePortfolio()` for positions; reads `useStore(s => s.prices)` for live overrides.
       - Renders a `<Table>` (from `components/ui/table.tsx`) with columns: Ticker | Qty | Avg Cost | Current Price | Unrealized P&L | %.
       - For each position, color the Unrealized P&L and % cells green (pnl-up) or red (pnl-down).
       - Use `formatQuantity`, `formatDollars`, `formatPercent` from `lib/format.ts`.
       - Empty portfolio -> render a single row "No open positions".
    6. Update `frontend/src/app/globals.css` (additive): keep the @theme block; add `.text-pnl-up { color: var(--color-pnl-up); }` and `.text-pnl-down { color: var(--color-pnl-down); }` utility shortcuts (Tailwind v4 auto-generates these from --color-pnl-up, --color-pnl-down already, but the explicit shortcut guarantees availability).
    7. Run `cd frontend && npx tsc --noEmit` to confirm types align.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npx tsc --noEmit 2>&1 | tail -10 && echo '---' && ls frontend/src/components/Header.tsx frontend/src/components/PortfolioHeatmap.tsx frontend/src/components/PnLChart.tsx frontend/src/components/PositionsTable.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All four component files exist under frontend/src/components/
    - All three UI primitive files (button.tsx, input.tsx, table.tsx) exist
    - Header.tsx imports usePortfolio AND useStore AND usePriceStream
    - PortfolioHeatmap.tsx uses SVG (<rect>) with fillOpacity derived from pnl_percent
    - PnLChart.tsx imports lightweight-charts and uses addAreaSeries + series.update
    - PositionsTable.tsx uses the local <Table> primitives from components/ui/table.tsx
    - `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Header, PortfolioHeatmap, PnLChart, and PositionsTable components render correctly with live data; UI primitives (button, input, table) are available for the trade bar and chat panel.</done>
</task>

<task type="auto">
  <name>Task 2: Build TradeBar and ChatPanel with inline action confirmations</name>
  <files>
    - frontend/src/components/TradeBar.tsx
    - frontend/src/components/ChatPanel.tsx
    - frontend/src/lib/hooks/useChat.ts
  </files>
  <read_first>
    - Re-read `backend/app/api/schemas.py` to confirm `ChatEndpointResponse.actions_executed` shape (type, ticker, status, detail, side, quantity, action).
    - Re-read `backend/app/api/portfolio.py` POST /api/portfolio/trade for the error message format: insufficient cash returns "Insufficient cash: need $X.XX, have $Y.YY"; insufficient shares returns "Insufficient shares for TICKER: need N, have M".
  </read_first>
  <action>
    1. Create `frontend/src/components/TradeBar.tsx`:
       - Local state: `ticker: string`, `quantity: string` (string-typed for input control), `error: string | null`, `loading: boolean`.
       - Two `<Input>` fields side by side (ticker uppercase on blur), two `<Button>` (Buy = default blue-primary, Sell = destructive red).
       - On submit: preventDefault; if quantity is empty or <= 0, set error "Quantity must be positive"; else call `executeTrade({ ticker: ticker.toUpperCase(), quantity: parseFloat(quantity), side: "buy" or "sell" })`.
       - On success: clear inputs, clear error, call `usePortfolio().mutate()` and `useWatchlist().mutate()` so the new position/cash reflects immediately. Show a transient success indicator (e.g., the new cash balance) for ~1.5s.
       - On error: catch the thrown Error, set `error` to `err.message` (which contains the server's detail string). Do NOT clear inputs.
       - Layout: `<div className="flex items-center gap-2 p-2 bg-bg-elevated rounded border border-border-muted">` with `<div className="text-pnl-down text-sm">{error}</div>` rendered below the buttons when error is set.
    2. Create `frontend/src/lib/hooks/useChat.ts`:
       - Local state: `messages: Array<{ id: string; role: "user" | "assistant"; content: string; actions: ChatActionResult[] }>`, `loading: boolean`, `error: string | null`.
       - `sendMessage(content: string)`: pushes a local user message immediately (id = crypto.randomUUID()), sets loading=true, calls `sendChat({ message: content })`, on success pushes the assistant message with `content: response.message` and `actions: response.actions_executed`. On error, pushes an assistant error message.
       - After a successful response, calls `usePortfolio().mutate()` and `useWatchlist().mutate()` (best-effort; ignore errors) so trades/watchlist changes in the response are reflected immediately in the rest of the UI.
       - Returns `{ messages, loading, error, sendMessage }`.
       - On mount, do NOT auto-load any history (per Plan 04-01 there's no GET /api/chat/history endpoint; the chat starts empty each session — this is intentional, mirrors product spec "scrollable conversation history" with no persistence requirement for v1).
    3. Create `frontend/src/components/ChatPanel.tsx`:
       - Uses `useChat()`.
       - Renders: a scrollable message list (`<div className="flex-1 overflow-y-auto">`) with each message as a `<div>` with role-tinted background (user = bg-blue-primary text-white, assistant = bg-bg-elevated). Auto-scroll to bottom on new message via `useEffect` + ref scrollTop.
       - For assistant messages, after the text content render a `<div className="flex flex-wrap gap-1 mt-2">` of inline action chips:
         - For `type === "trade" && status === "executed"`: chip "Bought/Sold Q TICKER @ $PRICE" with bg-pnl-up (or bg-pnl-down for sells).
         - For `type === "watchlist" && status === "executed"`: chip "+ TICKER added" or "- TICKER removed" with bg-purple-secondary.
         - For any action with `status === "failed"`: chip "FAILED: TICKER — detail" with bg-pnl-down.
       - Loading state: render a small "FinAlly is thinking..." indicator with a pulsing dot.
       - Input area at the bottom: `<Input>` text field + `<Button>` (Submit variant, purple-secondary) labeled "Send". On submit, call `sendMessage(inputValue)` then clear input. Disable input + button while `loading`.
       - Layout: `<aside className="flex flex-col h-full w-80 bg-bg-elevated border-l border-border-muted">`.
       - Collapsible: include a small chevron toggle at the top to collapse the panel to a 32px-wide rail with just a chat icon (lucide-react `MessageSquare`). When collapsed, only the toggle + icon visible; clicking expands.
    4. Run `cd frontend && npx tsc --noEmit` and `cd frontend && npx eslint src/ --max-warnings 0` to confirm types and lint are clean.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npx tsc --noEmit 2>&1 | tail -10 && echo '---' && cd frontend && npx eslint src/ --max-warnings 0 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TradeBar.tsx exists and calls executeTrade + usePortfolio().mutate + useWatchlist().mutate on success
    - TradeBar.tsx displays the server's detail string in a `.text-pnl-down` element on 4xx
    - ChatPanel.tsx exists and renders user/assistant messages with role-tinted backgrounds
    - ChatPanel.tsx renders inline action chips for trade/watchlist executed/failed from ChatActionResult array
    - ChatPanel.tsx shows a loading indicator while useChat().loading is true
    - useChat.ts calls usePortfolio().mutate() and useWatchlist().mutate() after a successful sendChat response
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint src/ --max-warnings 0` exits 0
  </acceptance_criteria>
  <done>TradeBar accepts ticker + quantity, executes buy/sell via POST /api/portfolio/trade, and displays inline validation errors; ChatPanel sends messages, displays assistant responses with inline action confirmations, and refreshes portfolio/watchlist caches after each successful response.</done>
</task>

<task type="auto">
  <name>Task 3: Compose all six new components into the dashboard layout</name>
  <files>
    - frontend/src/app/page.tsx
    - frontend/src/app/globals.css
  </files>
  <read_first>
    - Confirm the existing page.tsx from 04-01 still composes WatchlistPanel + MainChart; this task adds Header at top, PortfolioHeatmap + PnLChart + PositionsTable + TradeBar in a new region, and ChatPanel as a right-side aside. Do NOT remove existing WatchlistPanel or MainChart imports.
  </read_first>
  <action>
    1. Update `frontend/src/app/page.tsx`:
       - Keep `"use client"` directive.
       - Layout: a CSS grid with the following regions (Tailwind classes):
         - Row 1 (header): `<Header />` spanning the full width.
         - Row 2 (main): grid-cols-[300px_1fr]; left = WatchlistPanel (300px); right = MainChart (flex-1, h-[480px]).
         - Row 3 (portfolio): grid-cols-3; col 1 = PortfolioHeatmap (h-64); col 2 = PnLChart (h-64); col 3 = PositionsTable (h-64, scrollable overflow-y-auto).
         - Row 4 (trade + chat): grid-cols-[1fr_320px]; left = TradeBar (sticky to bottom of its region); right = ChatPanel (h-[480px]).
         - Total page height: `h-screen overflow-hidden` so each region scrolls independently rather than the whole page.
       - Use `bg-bg-base text-text-primary` on the page root.
       - Import all new components: `import { Header } from "@/components/Header";` etc. Use `import` statements for each; do NOT use dynamic imports.
       - Wrap the page in a single `<main className="grid grid-rows-[auto_1fr_auto_auto] h-screen overflow-hidden">`.
    2. Update `frontend/src/app/globals.css` (additive): no new tokens needed; the existing `@theme` block from 04-01 covers everything. Verify by reading the file.
    3. Run `cd frontend && npm run build` — must produce frontend/out/ and exit 0. If TypeScript or ESLint errors appear, fix them. Common issues: missing dependency declarations (none expected — all deps installed in 04-01), incorrect import paths (must use `@/...` alias), or unused-variable lint warnings.
    4. After the build succeeds, do a manual smoke test:
       - Start the backend: `cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 &` (already running in dev sessions).
       - Open `http://localhost:8000/api/health` in browser — should return `{"status":"ok"}`.
       - Static export will be served by FastAPI in 04-03; for now confirm `ls frontend/out/` shows index.html.
    5. Confirm `cd frontend && npx tsc --noEmit && cd frontend && npx eslint src/ --max-warnings 0 && cd frontend && npm run build` all exit 0 in sequence.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/frontend && npx tsc --noEmit 2>&1 | tail -10 && echo '---' && cd frontend && npx eslint src/ --max-warnings 0 2>&1 | tail -10 && echo '---' && cd frontend && npm run build 2>&1 | tail -15 && echo '---' && ls frontend/out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx imports Header, PortfolioHeatmap, PnLChart, PositionsTable, TradeBar, ChatPanel AND retains WatchlistPanel + MainChart imports
    - page.tsx uses a single `<main>` wrapper with `grid grid-rows-[auto_1fr_auto_auto] h-screen overflow-hidden` (or equivalent CSS Grid with four rows)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx eslint src/ --max-warnings 0` exits 0
    - `cd frontend && npm run build` exits 0 and produces frontend/out/index.html
  </acceptance_criteria>
  <done>The dashboard layout now renders Header + WatchlistPanel + MainChart + PortfolioHeatmap + PnLChart + PositionsTable + TradeBar + ChatPanel in a single dark-themed CSS-grid composition. All 23 v1 requirements from PF, TB, CHAT, and HDR families are addressed.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd frontend && npm run build` exits 0
2. `cd frontend && npx tsc --noEmit` exits 0
3. `cd frontend && npx eslint src/ --max-warnings 0` exits 0
4. With backend running on port 8000 and the page loaded, executing a buy of 1 AAPL via the trade bar reduces cash, adds a position, updates the heatmap, the positions table, and the P&L chart data points (after the snapshot loop records the post-trade value)
5. Sending a chat message returns a response with at least one action_executed entry reflected inline; LLM_MOCK=true mode (via docker) yields deterministic responses
</verification>

<success_criteria>
- All 23 requirements (PF-01..05, TB-01..07, CHAT-01..07, HDR-01..03) are implemented and verifiable
- No regressions in 04-01 outputs: WatchlistPanel, MainChart, sparklines, price flashes, watchlist CRUD still work
- The dashboard at http://localhost:8000 (when served by FastAPI) shows all six new regions populated with live data
</success_criteria>

<output>
Create `.planning/phases/04-frontend-docker-testing/04-02-SUMMARY.md` when done
</output>
