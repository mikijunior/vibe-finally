---
phase: 04-frontend-docker-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - .env.example
  - scripts/start_mac.sh
  - scripts/stop_mac.sh
  - scripts/start_windows.ps1
  - scripts/stop_windows.ps1
  - scripts/README.md
  - backend/app/main.py
  - test/docker-compose.test.yml
  - test/playwright.config.ts
  - test/package.json
  - test/tsconfig.json
  - test/e2e/watchlist.spec.ts
  - test/e2e/trade.spec.ts
  - test/e2e/portfolio.spec.ts
  - test/e2e/chat.spec.ts
  - test/e2e/sse-resilience.spec.ts
  - test/e2e/fixtures.ts
  - test/README.md
  - .github/workflows/e2e.yml
autonomous: true
requirements:
  - DOCKER-01
  - DOCKER-02
  - DOCKER-03
  - DOCKER-04
  - DOCKER-05
  - DOCKER-06
  - DOCKER-07
  - DOCKER-08
  - DOCKER-09
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - A single `docker build -t finally .` produces a container image that, when run with `docker run -p 8000:8000 -v finally-data:/app/db --env-file .env finally`, exposes the FinAlly workstation at http://localhost:8000
    - FastAPI serves both `/api/*` (REST + SSE) and `/*` (Next.js static export from `frontend/out/`) on the same port 8000 with no CORS configuration
    - SQLite data persists across container restarts via the named Docker volume `finally-data` mounted at `/app/db`
    - Start scripts (mac/Linux + Windows PowerShell) are idempotent — re-running on a healthy container is a no-op; printing the URL is consistent
    - Playwright E2E tests run against the Docker stack via `docker compose -f test/docker-compose.test.yml up --abort-on-container-exit` and exit 0 on success; they exercise watchlist CRUD, trade execution, portfolio display, AI chat with LLM_MOCK=true, and SSE reconnection
    - A CI workflow at `.github/workflows/e2e.yml` runs the E2E tests on every PR and uploads the Playwright report on failure
  artifacts:
    - Dockerfile (multi-stage: node:20-slim -> python:3.12-slim with uv)
    - .dockerignore (exclude node_modules, .next, out, .venv, .git, planning/, frontend tests)
    - docker-compose.yml (single-service wrapper around the image)
    - .env.example (OPENROUTER_API_KEY=, MASSIVE_API_KEY=, LLM_MOCK=false)
    - scripts/start_mac.sh, scripts/stop_mac.sh, scripts/start_windows.ps1, scripts/stop_windows.ps1 (idempotent, executable)
    - scripts/README.md (short usage instructions)
    - backend/app/main.py modified to mount frontend/out/ on "/" with html=True
    - test/docker-compose.test.yml (app + playwright services, LLM_MOCK=true)
    - test/playwright.config.ts (baseURL=http://app:8000, chromium only)
    - test/package.json, test/tsconfig.json (Playwright deps)
    - test/e2e/watchlist.spec.ts, trade.spec.ts, portfolio.spec.ts, chat.spec.ts, sse-resilience.spec.ts
    - test/e2e/fixtures.ts (custom test fixture with reset helpers)
    - test/README.md (how to run the suite locally)
    - .github/workflows/e2e.yml (CI workflow)
  key_links:
    - Dockerfile stage 2 copies backend/app/, runs uv sync --frozen, then COPYs frontend/out from stage 1 -> /app/static; CMD runs uvicorn on 0.0.0.0:8000
    - backend/app/main.py: app.mount("/", StaticFiles(directory="static", html=True)) AFTER all API routers so /api/* matches first (FastAPI matches in registration order)
    - docker run --env-file .env picks up OPENROUTER_API_KEY, MASSIVE_API_KEY, LLM_MOCK; LLM_MOCK=true in test/docker-compose.test.yml forces deterministic mock responses
    - test/docker-compose.test.yml: app service uses build context .. ; playwright service uses mcr.microsoft.com/playwright:v1.49.0-jammy and runs `npx playwright test --reporter=line` against http://app:8000
    - Playwright tests use page.goto("/") and verify DOM state; chat.spec.ts uses LLM_MOCK=true and asserts on deterministic mock response text + action_executed chips
---

<objective>
Wrap the FinAlly application in a Docker container, add cross-platform start/stop scripts, mount the FastAPI static frontend on `/`, and add Playwright E2E tests that run against the containerized stack. This is the final plan of the milestone and the only one that touches deployment / CI.

Purpose: Ships the one-command student experience — a single Docker image with all functionality (frontend + backend + SSE + LLM + persistence) — and proves it works end-to-end with automated tests.

Output: A multi-stage Dockerfile, docker-compose.yml, start/stop scripts for macOS/Linux and Windows, a static-file mount in FastAPI, a Playwright test suite covering the critical user flows, and a CI workflow that runs the suite on every PR.
</objective>

<execution_context>
@/Users/mijunior/vibecode/finally/.claude/gsd-core/workflows/execute-plan.md
@/Users/mijunior/vibecode/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/mijunior/vibecode/finally/.planning/PROJECT.md
@/Users/mijunior/vibecode/finally/.planning/ROADMAP.md
@/Users/mijunior/vibecode/finally/.planning/STATE.md
@/Users/mijunior/vibecode/finally/.planning/REQUIREMENTS.md
@/Users/mijunior/vibecode/finally/planning/PLAN.md
@/Users/mijunior/vibecode/finally/backend/app/main.py
@/Users/mijunior/vibecode/finally/backend/pyproject.toml
@/Users/mijunior/vibecode/finally/.planning/phases/04-frontend-docker-testing/04-01-PLAN.md
@/Users/mijunior/vibecode/finally/.planning/phases/04-frontend-docker-testing/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write multi-stage Dockerfile, mount FastAPI on /, .env.example, .dockerignore, docker-compose.yml</name>
  <files>
    - Dockerfile
    - .dockerignore
    - docker-compose.yml
    - .env.example
    - backend/app/main.py
  </files>
  <read_first>
    - Re-read backend/app/main.py to understand current router registration order: chat_router, portfolio_router, system_router, watchlist_router, plus the SSE stream router. The static mount on "/" must be added LAST so /api/* routes match first (FastAPI/Starlette resolves routes in registration order).
    - Re-read backend/pyproject.toml to confirm the entry point: `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000` is the correct CMD.
    - Confirm frontend/out exists (produced by 04-01 and 04-02 plans' `npm run build`). The Dockerfile's stage 1 must run `npm run build` to produce it; do not assume a pre-built directory is checked in.
  </read_first>
  <action>
    1. Create `/Dockerfile` at the project root with the following content (two stages):
       ```
       # syntax=docker/dockerfile:1.7
       # Stage 1: Build Next.js static export
       FROM node:20-slim AS frontend-build
       WORKDIR /build
       COPY frontend/package.json frontend/package-lock.json* ./frontend/
       WORKDIR /build/frontend
       RUN npm ci || npm install
       COPY frontend/ ./
       RUN npm run build       # produces /build/frontend/out

       # Stage 2: Python runtime with uv
       FROM python:3.12-slim AS runtime
       ENV PYTHONDONTWRITEBYTECODE=1 PYTHONUNBUFFERED=1
       RUN pip install --no-cache-dir uv==0.5.11
       WORKDIR /app
       COPY backend/pyproject.toml backend/uv.lock* ./backend/
       WORKDIR /app/backend
       RUN uv sync --frozen --no-dev || uv sync --no-dev
       COPY backend/ ./
       # Copy the built static export from stage 1
       COPY --from=frontend-build /build/frontend/out /app/static
       RUN mkdir -p /app/db
       EXPOSE 8000
       WORKDIR /app/backend
       CMD ["uv", "run", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
       ```
       The `||` fallback in npm ci and uv sync lets the build succeed even when lockfiles are regenerated mid-development; the primary path uses `npm ci` / `uv sync --frozen` for reproducibility.
    2. Create `/.dockerignore` at the project root excluding: `.git`, `.github`, `.planning`, `.claude`, `.pytest_cache`, `.ruff_cache`, `node_modules`, `frontend/node_modules`, `frontend/.next`, `frontend/out` (built inside stage 1, not copied from host), `backend/.venv`, `backend/.pytest_cache`, `backend/__pycache__`, `__pycache__`, `.env`, `db/*.db`, `test/`, `scripts/`, `planning/`. Keep `backend/`, `frontend/`, `Dockerfile`, `docker-compose.yml`, `.env.example`.
    3. Modify `backend/app/main.py`:
       - Add `from fastapi.staticfiles import StaticFiles` at the top.
       - After all `app.include_router(...)` calls and the `@app.get("/health")` handler, add:
         ```python
         app.mount("/", StaticFiles(directory="/app/static", html=True), name="static")
         ```
         IMPORTANT: this mount must be the LAST registration so /api/* routes resolve first (Starlette matches in order). The directory `/app/static` is created by the Dockerfile (COPY --from=frontend-build ... /app/static).
       - For dev mode (when running uvicorn outside Docker with no `/app/static` present), wrap the mount in a `try`/`except` that logs a warning and skips if the directory doesn't exist. Use `import os` + `os.path.isdir("/app/static")`.
    4. Create `/.env.example` at the project root (committed, no real keys):
       ```
       # Required for AI chat functionality. Get a key at https://openrouter.ai
       OPENROUTER_API_KEY=

       # Optional: real market data. Leave empty to use the built-in simulator.
       # MASSIVE_API_KEY=

       # Set to "true" for deterministic mock LLM responses (testing).
       LLM_MOCK=false
       ```
    5. Create `/docker-compose.yml` at the project root:
       ```yaml
       services:
         app:
           build: .
           image: finally
           container_name: finally
           ports:
             - "8000:8000"
           env_file:
             - .env
           volumes:
             - finally-data:/app/db
           restart: unless-stopped
       volumes:
         finally-data:
       ```
    6. Run `docker --version` to confirm Docker is installed; do NOT actually run `docker build` here (it's slow and requires Docker daemon). The executor in 04-03 will run the build.
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally && docker --version 2>&1 && echo '---' && [ -f Dockerfile ] && [ -f .dockerignore ] && [ -f .env.example ] && [ -f docker-compose.yml ] && echo 'all files present' && echo '---' && grep -c 'app.mount' backend/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Dockerfile exists at project root with two stages (node:20-slim frontend-build, python:3.12-slim runtime) and a final CMD running uvicorn
    - .dockerignore exists and lists node_modules, .git, .planning, .env, db/*.db, etc.
    - .env.example exists with OPENROUTER_API_KEY, MASSIVE_API_KEY (commented), LLM_MOCK=false
    - docker-compose.yml exists with a single `app` service mapping port 8000 and mounting the `finally-data` volume at /app/db
    - backend/app/main.py includes `app.mount("/", StaticFiles(...))` AFTER all API router registrations, with a try/except guard so dev mode (no /app/static) doesn't crash
    - `docker --version` reports a version (i.e., Docker is available)
  </acceptance_criteria>
  <done>Multi-stage Dockerfile, .dockerignore, .env.example, docker-compose.yml, and the FastAPI static mount on "/" are all in place; the application can be built and run with a single `docker compose up` command.</done>
</task>

<task type="auto">
  <name>Task 2: Write start/stop scripts for macOS/Linux and Windows, plus README</name>
  <files>
    - scripts/start_mac.sh
    - scripts/stop_mac.sh
    - scripts/start_windows.ps1
    - scripts/stop_windows.ps1
    - scripts/README.md
  </files>
  <read_first>
    - Re-read Dockerfile and docker-compose.yml from Task 1 to confirm exact image name (`finally`), service name (`app` for compose, `finally` for plain docker), and container name (`finally`).
    - On macOS/Linux, scripts must be `chmod +x` after writing; PowerShell scripts use `.ps1` extension and execute via `pwsh` or Windows PowerShell.
  </read_first>
  <action>
    1. Create `scripts/start_mac.sh` (macOS/Linux bash, executable). Behavior:
       - `#!/usr/bin/env bash` and `set -euo pipefail`.
       - From the project root (use `cd "$(dirname "$0")/.."`).
       - Check `docker --version` is present; if not, print error and exit 1.
       - Check if container `finally` is already running (`docker ps -q -f name=finally`); if so, print "Container 'finally' is already running at http://localhost:8000" and exit 0.
       - If image `finally` does not exist (`docker image inspect finally >/dev/null 2>&1`), run `docker compose -f docker-compose.yml -p finally build` (this uses the compose file for consistent env_file + volume setup).
       - Run `docker compose -f docker-compose.yml -p finally up -d` (detached). The compose file handles env_file and volume automatically.
       - Wait up to 30s for the health endpoint: `for i in {1..30}; do curl -fsS http://localhost:8000/api/health >/dev/null && break || sleep 1; done`.
       - Print "FinAlly is running at http://localhost:8000" (and note the browser will open automatically on macOS via `open` if `$BROWSER` env is set; otherwise just print the URL).
       - On any failure, print the error and exit 1.
    2. Create `scripts/stop_mac.sh`:
       - `#!/usr/bin/env bash` and `set -euo pipefail`.
       - From project root.
       - Run `docker compose -f docker-compose.yml -p finally down` (preserves the `finally-data` volume; if no compose project exists, this is a no-op).
       - Optionally `docker rm -f finally 2>/dev/null || true` to handle the case where the container was started via plain `docker run` (the executor's plan should use compose, but defensive cleanup is fine).
       - Print "FinAlly stopped. Data volume 'finally-data' preserved."
    3. Create `scripts/start_windows.ps1` (PowerShell, idempotent):
       - At top: `#Requires -Version 5.1`.
       - From `$PSScriptRoot\..`.
       - Check `docker --version`; exit 1 if missing.
       - Check `docker ps -q -f name=finally`; if non-empty, print "Already running" and exit 0.
       - If `docker image inspect finally` is null, run `docker compose -f docker-compose.yml -p finally build`.
       - Run `docker compose -f docker-compose.yml -p finally up -d`.
       - Wait up to 30s: `for ($i=1; $i -le 30; $i++) { try { Invoke-WebRequest -Uri http://localhost:8000/api/health -UseBasicParsing | Out-Null; break } catch { Start-Sleep 1 } }`.
       - Print "FinAlly is running at http://localhost:8000".
    4. Create `scripts/stop_windows.ps1`:
       - `docker compose -f docker-compose.yml -p finally down`; print "FinAlly stopped. Data volume preserved."
    5. After writing the .sh files, run `chmod +x scripts/start_mac.sh scripts/stop_mac.sh` so they're executable.
    6. Create `scripts/README.md` (short usage notes):
       ```
       # FinAlly Start/Stop Scripts

       ## macOS / Linux
       - `./scripts/start_mac.sh` — build (if needed) and start the container; prints the URL when ready.
       - `./scripts/stop_mac.sh` — stop and remove the container; preserves the data volume.

       ## Windows (PowerShell)
       - `.\scripts\start_windows.ps1` — build (if needed) and start the container.
       - `.\scripts\stop_windows.ps1` — stop and remove the container.

       ## Data persistence
       The SQLite database is stored in the `finally-data` Docker volume mounted at `/app/db` inside the container. Stopping the container preserves the volume; deleting it (`docker volume rm finally-data`) resets the app to a fresh state.

       ## Configuration
       Copy `.env.example` to `.env` and fill in `OPENROUTER_API_KEY` for AI chat. `MASSIVE_API_KEY` is optional (default: simulator). `LLM_MOCK=true` returns deterministic mock LLM responses.
       ```
    7. Verify by running shellcheck on the .sh files if available, otherwise just `bash -n scripts/start_mac.sh && bash -n scripts/stop_mac.sh` (syntax check only).
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally && bash -n scripts/start_mac.sh && bash -n scripts/stop_mac.sh && [ -x scripts/start_mac.sh ] && [ -x scripts/stop_mac.sh ] && echo 'sh scripts OK' && echo '---' && ls scripts/</automated>
  </verify>
  <acceptance_criteria>
    - All four scripts exist under scripts/ and the .sh files are executable (`chmod +x` applied)
    - `bash -n` on each .sh script exits 0 (syntax check)
    - scripts/start_mac.sh: idempotent (skips if container is running), waits for /api/health, prints URL
    - scripts/stop_mac.sh: runs `docker compose down` (preserves volume)
    - scripts/start_windows.ps1 + stop_windows.ps1 exist and follow the PowerShell equivalent pattern
    - scripts/README.md exists with usage for both platforms
  </acceptance_criteria>
  <done>Start/stop scripts are idempotent on macOS/Linux and Windows; users can launch the full FinAlly workstation with a single command on either platform.</done>
</task>

<task type="auto">
  <name>Task 3: Build Playwright test suite (docker-compose.test.yml, configs, 5 spec files, CI workflow)</name>
  <files>
    - test/docker-compose.test.yml
    - test/playwright.config.ts
    - test/package.json
    - test/tsconfig.json
    - test/e2e/fixtures.ts
    - test/e2e/watchlist.spec.ts
    - test/e2e/trade.spec.ts
    - test/e2e/portfolio.spec.ts
    - test/e2e/chat.spec.ts
    - test/e2e/sse-resilience.spec.ts
    - test/README.md
    - .github/workflows/e2e.yml
  </files>
  <read_first>
    - Re-read backend/app/api/portfolio.py + chat.py + watchlist.py to confirm exact REST shapes (status codes, error fields) that tests will assert against.
    - Confirm the frontend output from 04-01 + 04-02: tests reference components by accessible roles/text (e.g., `getByRole('row', { name: /AAPL/i })`). If selectors aren't stable, the executor should add `data-testid` attributes to the relevant components in a small follow-up edit.
  </read_first>
  <action>
    1. Create `test/docker-compose.test.yml` (referenced from the project root):
       ```yaml
       services:
         app:
           build:
             context: ..
             dockerfile: Dockerfile
           image: finally:test
           container_name: finally-test-app
           environment:
             - LLM_MOCK=true
             - OPENROUTER_API_KEY=mock-not-used
           expose:
             - "8000"
           healthcheck:
             test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/health"]
             interval: 2s
             timeout: 3s
             retries: 15
         playwright:
           image: mcr.microsoft.com/playwright:v1.49.0-jammy
           container_name: finally-test-pw
           depends_on:
             app:
               condition: service_healthy
           working_dir: /tests
           volumes:
             - ./:/tests
           command: ["npx", "playwright", "test", "--reporter=line"]
       ```
    2. Create `test/package.json`:
       ```json
       {
         "name": "finally-e2e",
         "private": true,
         "type": "module",
         "scripts": { "test": "playwright test" },
         "devDependencies": {
           "@playwright/test": "1.49.0",
           "typescript": "5.6.3"
         }
       }
       ```
    3. Create `test/tsconfig.json` (standard Node ESM config):
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ES2022",
           "moduleResolution": "Bundler",
           "esModuleInterop": true,
           "strict": true,
           "skipLibCheck": true
         },
         "include": ["**/*.ts"]
       }
       ```
    4. Create `test/playwright.config.ts`:
       ```typescript
       import { defineConfig } from "@playwright/test";
       export default defineConfig({
         testDir: "./e2e",
         timeout: 30_000,
         retries: 0,
         reporter: [["list"]],
         use: {
           baseURL: "http://app:8000",
           trace: "retain-on-failure",
           screenshot: "only-on-failure",
         },
         projects: [{ name: "chromium", use: { browserName: "chromium" } }],
       });
       ```
    5. Create `test/e2e/fixtures.ts` — custom test fixture using `@playwright/test`'s `test as base`, with helpers: `resetApp()` POSTs to `/watchlist/test-remove/{ticker}` for each default ticker then `POST /watchlist/test-add/{ticker}` to re-add them. This relies on the `TESTING=1` endpoints in main.py; add `TESTING: "1"` to test/docker-compose.test.yml app environment if not already present.
    6. Create `test/e2e/watchlist.spec.ts`:
       - Test 1 (`renders 10 default tickers`): goto /, assert at least 10 rows containing the default symbols (AAPL..NFLX).
       - Test 2 (`prices change over time`): goto /, capture price cell text, wait 3 seconds, capture again — assert at least one price changed.
       - Test 3 (`add ticker via input`): goto /, find the add-ticker input, type "BA", click Add, assert a new row with "BA" appears within 3 seconds.
       - Test 4 (`remove ticker via delete button`): goto /, find the AAPL row, click its delete button, assert AAPL row disappears within 3 seconds.
    7. Create `test/e2e/trade.spec.ts`:
       - Test 1 (`buy reduces cash and adds position`): goto /, capture initial cash, type "AAPL" + "1", click Buy, wait for response, assert cash decreased by approximately the current AAPL price, assert positions table has a row with AAPL qty=1.
       - Test 2 (`sell increases cash and removes position`): goto /, sell 1 share of AAPL, assert cash restored, assert AAPL row gone from positions table.
       - Test 3 (`insufficient cash shows inline error`): goto /, attempt to buy 999999 shares, assert "Insufficient cash" error appears within 2 seconds, assert cash unchanged.
    8. Create `test/e2e/portfolio.spec.ts`:
       - Prerequisite: an inline buy at the start to ensure at least one position exists.
       - Test 1 (`heatmap renders with at least one colored rectangle`): goto /, locate the heatmap SVG, assert at least one <rect> with non-transparent fill exists.
       - Test 2 (`P&L chart has at least one data point after snapshot`): goto /, wait 35 seconds (one snapshot interval), assert the P&L chart canvas has rendered pixels via page.locator('canvas').first().screenshot().
    9. Create `test/e2e/chat.spec.ts`:
       - Test 1 (`send message and receive mocked response with inline trade chip`): goto /, type "buy 1 AAPL", click Send, wait for assistant message, assert it contains text resembling the mock response and at least one trade chip is rendered.
    10. Create `test/e2e/sse-resilience.spec.ts`:
        - Test 1 (`status dot green after page load`): goto /, wait 2s for SSE, assert status dot has a green class/bg. Reload, assert status dot is green within 3s.
        - Test 2 (`status dot reflects forced disconnect`): use `page.route` to intercept `/api/stream/prices` and return an aborted response, wait 5s, assert status dot transitions to red or yellow.
    11. Create `test/README.md`:
        ```
        # FinAlly E2E Tests

        ## Running locally

        Build the app image and run the suite:
        ```
        docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright
        ```

        The suite runs against a fresh container with `LLM_MOCK=true` so tests are deterministic and don't require an OpenRouter key.

        ## What's tested

        - watchlist CRUD (default render, add, remove, live price updates)
        - trade execution (buy, sell, insufficient funds error)
        - portfolio display (heatmap, P&L chart with snapshot data)
        - AI chat (mocked LLM produces deterministic response with inline trade chips)
        - SSE resilience (status dot tracks EventSource readyState; reconnection on page reload)
        ```
    12. Create `.github/workflows/e2e.yml`:
        ```yaml
        name: E2E
        on:
          pull_request:
          push:
            branches: [main]
        jobs:
          e2e:
            runs-on: ubuntu-latest
            steps:
              - uses: actions/checkout@v4
              - name: Build + run E2E suite
                run: docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright
              - name: Upload Playwright report on failure
                if: failure()
                uses: actions/upload-artifact@v4
                with:
                  name: playwright-report
                  path: test/playwright-report/
        ```
    13. Run `cd test && npm install` to confirm playwright deps install cleanly (do not run `npx playwright install` — that's a 300MB browser download and only needed locally; the docker-compose.test.yml uses the official Playwright image with browsers preinstalled).
  </action>
  <verify>
    <automated>cd /Users/mijunior/vibecode/finally/test && [ -f docker-compose.test.yml ] && [ -f playwright.config.ts ] && [ -f package.json ] && [ -f tsconfig.json ] && ls e2e/ && echo '---' && cd test && npm install 2>&1 | tail -5 && echo '---' && cd test && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/docker-compose.test.yml exists with `app` (build from .., LLM_MOCK=true) and `playwright` (mcr.microsoft.com/playwright:v1.49.0-jammy) services
    - test/playwright.config.ts exists with baseURL `http://app:8000` and chromium project
    - test/package.json + test/tsconfig.json exist
    - All five spec files exist under test/e2e/ (watchlist, trade, portfolio, chat, sse-resilience) plus fixtures.ts
    - test/README.md exists with usage instructions
    - .github/workflows/e2e.yml exists and runs `docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright`
    - `cd test && npm install` exits 0
    - `cd test && npx tsc --noEmit` exits 0 (or shows only Playwright type strictness warnings tolerable for tests)
  </acceptance_criteria>
  <done>Playwright test suite, docker-compose.test.yml with healthcheck-driven startup, and the CI workflow are all in place; running `docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright` exercises the full FinAlly user flow end-to-end.</done>
</task>

</tasks>

<verification>
<verification>
After all tasks complete:
1. `docker build -t finally .` succeeds and produces an image (~500MB)
2. With LLM_MOCK=true and a freshly named volume, `docker run -d --name finally -v finally-test:/app/db -p 8000:8000 -e LLM_MOCK=true finally` then `curl http://localhost:8000/api/health` returns `{"status":"ok"}` and `curl http://localhost:8000/` returns the HTML shell
3. `scripts/stop_mac.sh` (or its PowerShell equivalent on Windows) cleanly stops and removes the container while preserving the volume
4. `docker compose -f test/docker-compose.test.yml up --abort-on-container-exit --exit-code-from playwright` exits 0 with all 5 spec files passing
5. `.github/workflows/e2e.yml` is syntactically valid (`actionlint` or visual review of the YAML) and uses the same compose file
</verification>

<success_criteria>
- All 12 requirements (DOCKER-01..09, TEST-01..03) are implemented and verifiable
- A student on macOS can clone the repo, run `scripts/start_mac.sh`, open `http://localhost:8000`, and see the full workstation within 5 minutes of clone
- E2E tests provide regression protection for the primary user flows; CI runs them on every PR
</success_criteria>

<output>
Create `.planning/phases/04-frontend-docker-testing/04-03-SUMMARY.md` when done
</output>
